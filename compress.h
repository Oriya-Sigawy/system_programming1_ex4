#ifndef COMPRESS_H
#define COMMPRES_H
int bin_to_txt(char* fileName);          //convert a binary file to a txt file, returns 1 if succeed, -1 for an error
int txt_to_bin(char* fileName);          //convert a binary file to a txt file, returns 1 if succeed, -1 for an error
#endif
